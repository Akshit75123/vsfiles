#include <stdio.h>
#include <stdlib.h>

struct node{
    int x;
    struct node *next;
};

struct node *START=NULL;

struct node 
struct node *t1, *t2;
while(START!=NULL){
    t2=START->next;
    start->next=t1;
    t1=START;
    START=t2;
    return t1;
}